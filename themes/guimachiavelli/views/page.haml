- if @page.index_page? 
  ~ @page.to_html(self)
- else
  - unless @page.articles.empty?
    %section.articles
      = article_summaries(@page.articles)
  - else
    %article.post.single
      = @page.to_html(self)

